# Build output
target/
*.class
dependency-reduced-pom.xml

# Allure commandline distribution (vendored binary, ~68 files) — install it
# locally via the allure CLI instead of committing the jars.
.allure/

# Maven
.mvn/
!.mvn/wrapper/maven-wrapper.properties

# IDE
.idea/
*.iml
.eclipse/
.settings/
.project
.classpath
*.DS_Store

# Logs
*.log
logs/

# Allure results (generated; commit only source, not output)
target/allure-results/
target/allure-report/

# Screenshots
target/screenshots/

# Copy .env.example to .env and fill in real values locally
.env
.env.local
*.env

# Secrets — never commit these
secrets.properties
credentials.properties
.vscode/settings.json
# Local Allure output at repo root
allure-results/
