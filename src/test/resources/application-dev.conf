# Environment Configuration — Development
# Inherits all defaults from application.conf; overrides specific values.

base {
  url = "https://www.saucedemo.com/"
}

browser {
  type     = "chromium"
  headless = false   # Run headed locally for debugging
}

api {
  base.url = "https://jsonplaceholder.typicode.com"
  timeout  = 15000
}

timeout {
  default = 30000
  wait    = 10000
}

log {
  level = "DEBUG"   # Verbose logging in dev
}

allure {
  enabled = true
}
